<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Camera permission for taking photos -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Storage permissions for reading images from gallery -->
    <!-- For Android 12 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" tools:replace="android:maxSdkVersion" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- For Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    
    <!-- Optional: For video if needed in the future -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    
    <!-- Camera feature declaration -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:screenOrientation="unspecified"
        android:resizeableActivity="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/LaunchTheme"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- BillGate result deep-link: BillGate's return-URL HTML can redirect
             to todaymall://billgate-result?... and the WebView intercepts it.
             Listing the scheme here is also useful so external apps can wake
             the merchant app via the same scheme if needed. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="todaymall" />
        </intent-filter>
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data
                android:scheme="https"
                android:host="todaymall.co.kr"
                android:pathPrefix="/signin/qr" />
        </intent-filter>
        <intent-filter android:autoVerify="true">
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data
                android:scheme="https"
                android:host="www.todaymall.co.kr"
                android:pathPrefix="/signin/qr" />
        </intent-filter>
      </activity>
    </application>

    <!-- Android 11+ package visibility: Linking.canOpenURL must be able to
         see Korean banking and simple-pay apps that BillGate launches via
         intent:// URLs. Without these queries Linking.canOpenURL always
         returns false on API 30+ and the launch silently fails. -->
    <queries>
        <!-- KakaoPay / KakaoTalk -->
        <package android:name="com.kakao.talk" />
        <package android:name="com.kakaopay.app" />
        <!-- NaverPay / Naver -->
        <package android:name="com.nhn.android.search" />
        <package android:name="com.naver.android.npay" />
        <!-- TossPay -->
        <package android:name="viva.republica.toss" />
        <!-- SamsungPay -->
        <package android:name="com.samsung.android.spay" />
        <package android:name="com.samsung.android.samsungpay.gear" />
        <!-- Payco -->
        <package android:name="com.nhnent.payapp" />
        <!-- ISP / Shinhan / KB / Hana / Lotte / NH / Hyundai / BC card apps -->
        <package android:name="kvp.jjy.MispAndroid320" />
        <package android:name="com.kbcard.cxh.appcard" />
        <package android:name="com.kbstar.kbbank" />
        <package android:name="com.shinhancard.smartshinhan" />
        <package android:name="com.shinhan.smartcaremgr" />
        <package android:name="com.hanaskcard.paycla" />
        <package android:name="com.hanaskcard.rocomo.potal" />
        <package android:name="com.lcacApp" />
        <package android:name="com.nh.cashcardapp" />
        <package android:name="com.nonghyup.android.bank" />
        <package android:name="nh.smart.nhallonepay" />
        <package android:name="com.hyundaicard.appcard" />
        <package android:name="com.wooricard.smartapp" />
        <package android:name="com.wooribank.smart.npib" />
        <package android:name="kr.co.citibank.citimobile" />
        <package android:name="com.smg.spbs" />
        <!-- Generic intent + market schemes the WebView may launch -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="intent" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
    </queries>
</manifest>
