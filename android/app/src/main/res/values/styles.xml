<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

    <!--
      Theme used while the activity launches and the JS bundle is still
      loading. windowBackground = the same logo + white that the JS splash
      shows, so the user sees one continuous splash instead of a blank window
      followed by the JS splash.
      MainActivity swaps to AppTheme in onCreate() so this drawable is not
      kept in memory or composited under every screen during normal use.
    -->
    <style name="LaunchTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/launch_screen</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

</resources>
